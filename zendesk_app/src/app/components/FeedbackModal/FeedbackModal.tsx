import { type JSX } from 'react'

import styles from './FeedbackModal.module.scss'

export const FeedbackModal = (): JSX.Element => {
  return <div className={styles.main_container}>Feedback</div>
}

export default FeedbackModal
